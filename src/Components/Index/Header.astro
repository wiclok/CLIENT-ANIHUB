<header>
  <div class="Content">
    <img src="AniHub-logo-trasparent.webp" alt="Logo De AniHub" />
    <div class="Containers-Buttons">
      <button class="Button-Login">Iniciar Sesión</button>
      <a class="Button-Register" href="/Registro">Registrarse</a>
    </div>
  </div>
</header>

<style>
  header {
    height: 10vh;
    background: rgba(0, 0, 0, 0.3);
    color: white;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-bottom: 1px solid #482f5d;
    display: flex;
    flex: row;
    justify-content: center;
    & .Content {
      width: 90%;
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      & img {
        height: 150px;
        width: 150px;
        object-fit: contain;
        margin-top: -38px;
      }
      & .Containers-Buttons {
        width: 25%;
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 2rem;
        & .Button-Login{
          height: 2.4rem;
          width: 8rem;
          border-radius: 10px;
          background: transparent;
          border: 1px solid #694B86;
          color: #fff;
          font-size: 1rem;
          transition: ease-in-out 0.3s all;
          cursor: pointer;
          &:hover {
            scale: 1.1;
            background-color: #3a1a5d;
            box-shadow: 0 5px 10px -5px #a844ff;
          }
        }
        & .Button-Register {
          height: 2.4rem;
          width: 8rem;
          border-radius: 10px;
          border: none;
          background: linear-gradient(90deg, #a844ff, #ff4ecd, #a844ff);
          background-size: 200% 100%;
          background-position: left;
          color: white;
          font-size: 1.1em;
          cursor: pointer;
          transition: all 0.3s ease-in-out;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          text-decoration: none;
          &:hover {
            background-position: right;
            scale: 1.1;
            box-shadow: 0 5px 10px -5px #a844ff;
          }
        }
      }
    }
  }
</style>
